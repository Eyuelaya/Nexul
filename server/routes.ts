// Backend routes removed: no server-side API routes remain.
// This placeholder ensures imports succeed but the app no longer registers routes.

export async function registerRoutes() {
  throw new Error("Backend API removed. No routes are registered.");
}
