// Backend storage removed. This module previously provided an in-memory
// storage implementation for user objects. It's been disabled because the
// project is now a static-only frontend.

export const storage = null as unknown as Record<string, never>;
